<% include ../partials/header %>
    <div class="row">
        <h1 style="text-align: center">Dodaj nową poradę do <%= game.name %></h1>
        <div style="width: 30%; margin: 25px auto;">
            <form action="/games/<%= game._id %>/comments" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="comment[text]" placeholder="Treść porady">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Zatwierdź!</button>
                </div>
            </form>
            <a href="/games">Powrót</a>
        </div>
    </div>
<% include ../partials/footer %>

